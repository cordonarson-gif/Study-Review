# 问题追踪

记录项目中发现的 Bug、待优化项和已解决的问题。

---

## 状态说明

| 标记 | 含义 |
|------|------|
| 🔴 未解决 | 问题仍存在，需要修复 |
| 🟡 部分解决 | 已做临时处理，但根因未消除 |
| 🟢 已解决 | 问题已彻底修复 |

---

## 问题列表

### #1 LaTeX 公式渲染失败（块级 & 行内）

- **发现时间**：2026-07-14
- **状态**：🟢 已解决
- **描述**：大模型返回的知识点中包含 `\(...\)` 行内公式和 `\[...\]` 块级公式，但原渲染管线（marked → sanitize → `<code>` 包装）不支持这些分隔符，且 marked 会先破坏公式中的 `\`、`_` 等字符，导致公式显示为乱码源码。
- **修复方案**：
  1. 集成 KaTeX 库，替代原来的 `<code>` 源码展示
  2. 重构渲染管线：先提取公式用占位符保护 → marked 渲染 markdown → KaTeX 渲染公式
  3. 同时支持 `$$...$$`、`\[...\]`、`$...$`、`\(...\)` 四种分隔符
- **涉及文件**：`src/app/App.tsx`、`src/main.tsx`、`src/styles/workspace.css`、`src/components/SceneRenderers.tsx`

---

### #2 "去录入题目"按钮点击无反应

- **发现时间**：2026-07-14
- **状态**：🟢 已解决
- **描述**：练习面板空状态时显示的"去录入题目"按钮，点击后只在状态栏显示一行提示文字，并未实际跳转到题目录入标签页，用户体验上像是"无反应"。
- **修复方案**：
  1. `PracticePanel` 新增 `onNavigateToImport` 回调 prop
  2. 按钮点击时同时调用 `onNavigateToImport()` 切换到 import tab
  3. `App.tsx` 中传入 `onNavigateToImport={() => setEditorTab('import')}`
- **涉及文件**：`src/components/PracticePanel.tsx`、`src/app/App.tsx`

---
### #3 图片录入功能非常糟糕，基本是在切除题目。（对于长文本题目）
