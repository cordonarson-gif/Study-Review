# 在线测验 / 作业出题批改测试记录

## 测试信息

- 测试日期：2026-07-19（Asia/Shanghai）
- 测试应用：Cram Engine Desktop 1.1.0
- 作业模式测试项目：`测验01`
- 作业模式项目模型：OpenAI Compatible / `deepseek-v4-pro`
- 题目录入对照项目：`计算机组成原理期末复习`
- 题目录入项目模型：`deepseek-reasoner`
- 说明：本轮没有重新调用模型生成题目，主要复核已有作业成果、共享题目导入、公式渲染和交付导出。

## 固定录入题目

```markdown
1. \(\displaystyle \int (3x^2 - 2x + 1)\,dx =\)（   ）
   A. \(x^3 - x^2 + x + C\)
   B. \(x^3 - x^2 + C\)
   C. \(6x - 2 + C\)
   D. \(x^3 - x + C\)
   Answer: A

2. \(\displaystyle \int \frac{1}{2x-3}\,dx =\)（   ）
   A. \(\ln|2x-3|+C\)
   B. \(\frac{1}{2}\ln|2x-3|+C\)
   C. \(2\ln|2x-3|+C\)
   D. \(\ln|x-1.5|+C\)
   Answer: B

3. $\displaystyle \int e^{2x}\,dx =$（   ）
   A. $e^{2x}+C$
   B. $2e^{2x}+C$
   C. $\frac{1}{2}e^{2x}+C$
   D. $e^x+C$
   Answer: C

4. \[\displaystyle \int_0^1 (2x+1)\,dx =\]（   ）
   A. 1
   B. 2
   C. 3
   D. 4
   Answer: B

5. $$\displaystyle \int_{-1}^{1} x^3\,dx =$$（   ）
   A. 0
   B. 1
   C. -1
   D. 2
   Answer: A
```

答案基线：`A、B、C、B、A`。

## 小功能逐点测试

### 1. 作业项目创建与模式选择

- 操作步骤：从项目列表重新打开此前通过新建项目向导创建的 `测验01`。
- 录入内容：项目模式为“作业出题批改 / 在线测验”，学科为数学。
- 预期结果：进入专属工作台，显示概览、题库、组卷、测验、批改、错题、反馈和交付 8 个页签。
- 实际结果：8 个页签均可进入，项目模型和模式信息显示正常。
- 结论：通过。

### 2. 作业模式内手工录入题目

- 操作步骤：依次检查作业模式的题库、组卷和测验页。
- 录入内容：计划录入上述 5 道积分题。
- 预期结果：教师可在作业项目内新增或编辑结构化题干、选项、答案和解析。
- 实际结果：作业模式的“题库”页仍是 Markdown 成果生成器，没有结构化题目编辑或确认入库入口。
- 结论：未通过。公式显示已修复，但作业模式仍缺少真正的题目数据录入工作流。

### 3. 五道积分题导入

- 操作步骤：进入通用“导题”页，选择“文本粘贴”，粘贴上述 5 道题，点击“识别并预览”；未点击确认入库。
- 录入内容：上述 5 道题，混合使用 `\(...\)`、`\[...\]`、`$...$`、`$$...$$`。
- 预期结果：拆分为 5 道题，保留公式、选项和答案。
- 实际结果：生成 5 个待入库草稿，未写入现有题库；答案字段依次为 `A、B、C、B、A`。
- 结论：部分通过。通用导题可用，但该入口没有集成到作业模式工作台。

### 4. 题库结构识别与选项保存

- 操作步骤：检查每个识别草稿的题干、4 个选项、答案来源和可入库状态。
- 录入内容：固定 5 道积分题。
- 预期结果：每题形成独立结构，选项 A-D 不串题，答案与基线一致。
- 实际结果：5 道题均被拆分，显示“可入库 / 题库原答案”；答案复核为 `A、B、C、B、A`。
- 结论：通过。

### 5. 题目校对与公式预览

- 操作步骤：在 5 个草稿中检查新增的“显示效果”区域。
- 录入内容：题干、选项、答案和解析中的积分、分式、上下标及积分限。
- 预期结果：源码输入框保持可编辑，显示效果区生成数学排版，不出现原始定界符。
- 实际结果：5 个显示效果区共生成 17 个 KaTeX 节点；未显示 `\(`、`\[` 或 `$$` 原始定界符。
- 结论：通过。

### 6. 试卷 / 作业组卷

- 操作步骤：进入 `测验01` 的“组卷”页，检查已有组卷成果及其预览。
- 录入内容：已有积分和积分中值定理选择题成果。
- 预期结果：题号、选项、答案表和公式能够阅读，Markdown 标题与强调标记被解析。
- 实际结果：组卷成果可在“预览”模式阅读，历史 LaTeX 内容已生成数学排版；仍然是 Markdown 成果，不是可拖拽组题的结构化试卷。
- 结论：部分通过。

### 7. 在线答题

- 操作步骤：进入 `测验01` 的“测验”页，查找学生作答、选择选项、提交和计时控件。
- 录入内容：已有在线测验结构成果。
- 预期结果：学生可以在应用内逐题作答并提交。
- 实际结果：页面展示的是 LMS 发布字段、自动判分规则、重试策略等 Markdown 文档，没有学生答题控件或提交记录。
- 结论：未通过。当前仍是“在线测验方案生成器”，不是实际在线测验系统。

### 8. 自动批改

- 操作步骤：进入“批改”页，查找真实学生答案、得分计算和批改执行按钮。
- 录入内容：已有评分规则成果。
- 预期结果：选择题提交后按答案基线自动计分，并保存作答证据。
- 实际结果：页面仅生成评分维度、分值规则和边界案例文档，没有真实学生提交数据，也没有执行批改的入口。
- 结论：未通过。评分规则可作为教师参考，但不能完成真实批改。

### 9. 错题标记与错题复盘

- 操作步骤：进入“错题”页，检查错题来源、学生作答和纠正任务。
- 录入内容：已有错题分析成果。
- 预期结果：错误分析引用真实题目 ID、学生选择和批改结果。
- 实际结果：页面有错误分布、原因和纠正任务，但交付信息显示结构化证据为 0 项，无法证明内容来自真实作答。
- 结论：部分通过。文档结构可用，数据可信度不足。

### 10. 解析与学情反馈

- 操作步骤：进入“反馈”页，检查反馈是否引用真实作答和错题证据。
- 录入内容：已有反馈成果。
- 预期结果：反馈基于真实答题记录，并能追溯到具体题目和错误答案。
- 实际结果：能生成亮点、薄弱点和练习建议，但当前项目没有真实学生提交链路，反馈证据不可追溯。
- 结论：部分通过。

### 11. 作业 / 测验产物公式预览

- 操作步骤：进入“题库”成果，切换到“编辑”；临时输入混合四类定界符的 Markdown，然后切回“预览”，不保存草稿。
- 录入内容：`\(...\)`、`\[...\]`、`$...$`、`$$...$$`，并包含 `## Integral Quiz` 和 `**1.**`。
- 预期结果：生成 KaTeX DOM；标题与加粗标记被 Markdown 解析；不同宽度无裁切或重叠。
- 实际结果：生成 10 个 KaTeX 节点，标题转成 H2，正文未显示 `##` 或 `**` 原始标记。在 1280px、1024px、480px 下均无预览区或页面横向溢出，公式节点未超出可视区域。
- 结论：通过。

### 12. 交付导出及内容复核

- 操作步骤：进入 `测验01` 的“交付”页，点击“导出交付包”，检查生成的 Markdown 和 JSON。
- 录入内容：已有作业单、评分规则、在线测验结构成果。
- 预期结果：生成两个文件，包含作业单、评分规则和在线测验结构。
- 实际结果：成功导出：
  - `C:\Users\ROG0\AppData\Roaming\CramEngineDesktop\projects\测验01-1784424268166\generated\测验01-delivery.md`
  - `C:\Users\ROG0\AppData\Roaming\CramEngineDesktop\projects\测验01-1784424268166\generated\测验01-delivery.json`
- 内容复核：Markdown 中存在“作业单”“评分规则”“在线测验结构”；交付状态为 3 项可交付、0 项待复核、1 项待补充、共 4 项，结构化证据仍为 0 项。
- 结论：部分通过。文件导出正常，但项目资料来源和真实测验证据仍缺失。

## 公式专项测试

- `\(...\)`：通过。
- `\[...\]`：通过。
- `$...$`：通过。
- `$$...$$`：通过。
- 分式、上下标、积分上下限、指数：通过。
- Markdown 标题与加粗：通过，不再原样显示 `##`、`**...**`。
- 行内代码与 fenced code block：自动化测试通过，代码中的公式定界符不会被渲染。
- 代码边界：自动化测试通过，转义反引号不会误触发代码状态，blockquote / list 内 fenced code 也保持源码展示。
- 非法公式：自动化测试通过，渲染函数不抛异常并保留可读回退内容。
- 安全清洗：自动化测试通过；原始 HTML 在进入 DOM 前会先清洗并转义，Markdown 链接和图片还会执行实体解码后的协议白名单校验，script、内联事件和 `javascript:` URL 不会形成可执行节点。
- 响应式：1280px、1024px、480px 运行时检查通过。

## 自动化验证结果

- `npm.cmd test`：通过，共 248 项。
  - Electron：119/119
  - Renderer：69/69
  - Settings UI：45/45
  - i18n：15/15
- `npm.cmd run typecheck`：通过。
- `npx.cmd vite build`：通过。
- 构建警告：主 JavaScript chunk 超过 500 kB；这是性能优化提示，不影响本次公式功能正确性。

## 总结

公式与 Markdown 展示问题已经修复：作业成果、导题预览、通用练习和教学游戏现在共用同一套 KaTeX 渲染层。

在线测验 / 作业出题批改整体仍不能视为完整可用的学生测评系统。当前强项是教师侧内容生成、评分规则和交付文档；主要缺口是作业模式内的结构化题库、学生答题入口、真实提交记录、自动计分执行及基于真实作答的错题和反馈闭环。
